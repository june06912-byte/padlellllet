# 초등학생용 음악 기초 학습 웹앱 개발 프롬프트 (개선판)

## 0. 개요 및 목적
초등학생(만 8~12세) 수준의 음악 초심자를 대상으로, **(A) 오선보 음이름 읽기**, **(B) 리듬 치기 게임(닉네임 로그인 + 랭킹 포함)**, **(C) 악보 업로드 → 리듬 악보 자동 생성/다운로드**, **(D) 오선보 작곡기(클릭 입력 + 재생)** 총 4가지 학습 모듈을 제공하는 반응형 웹앱을 개발한다. 추가로 **비밀번호 로그인 기반 "교사용" 관리 페이지**를 통해 교사가 위 기능들의 콘텐츠(곡 목록, 기본 패턴, 난이도 설정 등)를 언제든 추가·수정할 수 있게 한다. 아래 요구사항에 따라 먼저 전체 구조를 설명한 뒤, 실행 가능한 전체 코드를 제공하라.

---

## 1. 공통 요구사항

| 항목 | 세부 내용 |
|---|---|
| 대상 | 초등학생(만 8~12세) 수준, 악보를 처음 배우는 학습자 / 교사(관리자) |
| 플랫폼 | PC / 태블릿 / 모바일 반응형 웹 (뷰포트 360px~1920px 대응) |
| 언어/인터페이스 | 한글 UI, 음이름은 "도, 레, 미, 파, 솔, 라, 시, 도" 표기 |
| 디자인 톤 | 밝고 화려하지 않으면서 직관적인 UI, 큰 글씨·큰 버튼(터치 친화적, 최소 44px) |
| 화면 구조 | ① 상단: 제목 + 1~2줄 설명 + **모듈 전환 탭 4개('A. 음이름 읽기' / 'B. 리듬 치기' / 'C. 악보→리듬악보 변환' / 'D. 오선보 작곡기') + 현재 로그인한 닉네임 표시 + 우측 상단에 작은 자물쇠 아이콘('교사용')** <br> ② 중단: 학습(악보/게임/편집) 영역 <br> ③ 하단: 점수·피드백·통계 영역(C, D 탭에서는 다운로드·재생상태 영역으로 대체) |
| 접근성 | 색맹 고려(정답=초록만이 아니라 아이콘/텍스트 병기), 키보드 및 터치 모두 지원 |
| 오디오 | 실제 음원 파일 없이 **Web Audio API**로 사인파/간단 신디사이저 음 생성 (파일 의존성 제거). 메트로놈/리듬 미리듣기/사용자 입력 3가지 사운드를 서로 다른 음색으로 구분(3.7 참고) |
| 상태 관리 | 학생 학습 기록(오답, 진행 상황 등)은 새로고침 전까지 세션 내(메모리 변수)만 유지. **단, 교사가 등록한 곡·패턴·설정값(6장)과 리듬 치기 게임 랭킹(3.6)은 세션이 끝나도 유지되어야 하므로 `window.storage` 기반 영구 저장 방식을 사용** |

---

## 2. 기능 A: 도레미파솔라시도 오선보 읽기

### 2.1 악보 표시 규격 (모호함 제거)
- **높은음자리표(치음자리표) 고정 사용** (낮은음자리표는 이번 버전에서 제외, 추후 확장 고려)
- 표시 음역: **1옥타브, 가온다(C4=도) ~ 다음 옥타브 도(C5)** 8개 음
  - 도(C4, 아래 첫 번째 덧줄) ~ 도(C5, 셋째 줄 위 공간) 각 음의 오선 위치를 정확히 매핑한 SVG 좌표 테이블을 코드 내 상수로 정의
- 음표는 **온음표 또는 4분음표 모양(머리만 단순화 가능)** 1개씩만 표시

### 2.2 진행 방식
- 출제 방식: **순차 모드(도→시 순서)** 와 **무작위 모드** 를 상단 토글로 선택 가능
- 총 문항 수: 기본 10문항(설정에서 5/10/15 선택 가능)
- 답안 입력 방식 두 가지 모두 지원, 사용자가 전환 가능:
  1. **보기 선택형**: 화면 하단에 "도 레 미 파 솔 라 시 도" 8개 버튼 제공, 클릭으로 답안 제출
  2. **키보드 입력형**: 아래 매핑 고정 사용
     | 음이름 | 키보드 키 |
     |---|---|
     | 도 | C |
     | 레 | D |
     | 미 | E |
     | 파 | F |
     | 솔 | G |
     | 라 | A |
     | 시 | B |
     (한글 두벌식 자판의 ㄷ,ㄹ,ㅁ 등은 사용하지 않고 영문 알파벳 키만 사용, 화면에 매핑표를 항상 노출)

### 2.3 피드백
- 제출 즉시 정답/오답 표시(정답: 초록 테두리+"정답이에요!" / 오답: 빨강 테두리+"다시 확인해볼까요?")
- **오답 시**: 해당 음이 오선 몇 번째 줄/칸에 있는지 화살표와 라벨로 3초간 하이라이트 표시 후 다음 문제로 진행
- 10문항(또는 선택한 문항 수) 종료 후:
  - 점수 = (정답 수 / 전체 문항 수) × 100
  - 등급 메시지 (2.4 등급 기준과 통일해 사용)
  - "다시 도전하기" 버튼 제공

### 2.4 등급 기준 (기능 B와 통일)
| 점수 | 등급 | 메시지 |
|---|---|---|
| 90~100 | A | "완벽해요! 오선보 마스터!" |
| 70~89 | B | "좋아요! 조금만 더 연습해봐요." |
| 50~69 | C | "기본기는 있어요. 몇 번 더 해볼까요?" |
| 0~49 | D | "처음엔 누구나 어려워요. 다시 도전!" |

---

## 3. 기능 B: 1~4마디 리듬 치기 게임

### 3.1 리듬 패턴 정의 (구체화)
- **박자 선택 가능**: 4/4 고정이 아니라, 사용자가 상단 드롭다운/토글에서 **2/4, 3/4, 4/4, 6/8, 12/8** 중 선택 가능하게 구현
  - 박자에 따라 한 마디의 총 박자 수와 메트로놈 강세 패턴(예: 6/8·12/8은 3개씩 묶은 큰박 기준 강-약-약)이 자동으로 달라져야 함
  - 박자 변경 시 진행 중이던 패턴은 초기화하고, 선택한 박자에 맞는 패턴 세트에서 다시 무작위 추출
- 사용 음표/쉼표(아래 3.1.1~3.1.6 실제 곡 리듬 반영으로 확장): 4분음표, 8분음표(2개 이어치기 포함), 점4분음표, 점2분음표, 2분음표, 4분쉼표, 8분쉼표, 붙임줄(타이), **악센트(>) 표시 음표** — 총 9종
- **마디 수 선택**: 사용자가 1/2/3/4마디 중 선택 (박자와 무관하게 유지)

#### 3.1.0 리듬 소스 선택 모드 (신규)
리듬 문제 출제 방식을 상단에서 두 가지 모드 중 하나로 선택하게 한다.

1. **기본 패턴 모드**: 박자만 선택하면, 해당 박자에 맞는 일반 연습용 패턴 배열(마디 수별 최소 5개 이상)에서 무작위 추출 (기존 방식 유지)
   - 예시(1마디, 2/4박자): `["4분음표,4분음표"]`, `["8분+8분,4분음표"]` 등
   - 예시(1마디, 4/4박자): `["4분음표,4분음표,4분음표,4분음표]"`, `["8분+8분,4분음표,4분쉼표,4분음표"]` 등
   - 예시(1마디, 3/4박자): `["4분음표,4분음표,4분음표"]`, `["8분+8분,8분+8분,4분음표"]` 등
   - 예시(1마디, 6/8박자): `["8분음표×6"]`, `["점4분음표,점4분음표"]` 등
   - 예시(1마디, 12/8박자): `["8분음표×3, 8분음표×3, 8분음표×3, 8분음표×3"]`(큰박 4개) 등
2. **곡 선택 모드(신규)**: **"악보 선택" 드롭다운**에서 실제 동요 제목을 고르면, 해당 곡에 맞는 박자로 자동 전환되고(사용자가 직접 박자를 바꿀 수 없도록 비활성화), 그 곡의 실제 리듬 패턴이 순서대로(무작위 아님, 곡의 프레이즈 순서 그대로) 출제된다.
   - 드롭다운 목록: **"아기 염소" (4/4)**, **"별 보며 달보며" (6/8)**, **"손을 잡고" (3/4)**, **"가을길" (4/4)**, **"악기놀이" (12/8, 자진모리장단)**, **"다섯 글자 예쁜말" (4/4)** — 추후 곡 추가를 고려해 배열/객체 형태로 확장 가능하게 설계
   - 곡 선택 시 마디 수 선택 UI는 "전체 곡 진행"(해당 곡의 프레이즈 전체를 순서대로) 또는 "구간 반복"(1~4마디 단위로 원하는 프레이즈만 골라 반복) 중 선택 가능하게 구성
   - 곡 선택 모드에서는 화면에 곡 제목과 마디 번호(예: "아기 염소 – 3번째 마디 / 총 24마디")를 함께 표시

#### 3.1.1 "아기 염소" 리듬 반영 (업로드 악보 분석, 4/4 전용 프리셋)
업로드된 악보(이해별 작사·이순형 작곡, "아기 염소")를 시각적으로 분석해 **음높이는 제외하고 리듬(음가)만** 추출했다. 곡은 4마디씩 6개 프레이즈로 구성되며, 각 프레이즈는 아래 공통 골격을 가진다(마지막 4번째 마디는 항상 "점2분음표 + 4분쉼표"로 마무리). *악보 해상도상 세부 음가는 육안 판독 기반의 근사치이므로, 정밀한 채보가 필요하면 원곡 악보와 다시 대조할 것을 권장.*

| 마디 위치 | 리듬 패턴(좌→우) | 비고 |
|---|---|---|
| 프레이즈 1번째 마디 | 8분+8분, 8분+8분, 4분음표, 2분음표 | "파란 하늘"류 (E-E-E-E-Q-H) |
| 프레이즈 2번째 마디 | 8분음표 연속 5~6개 + 점8분음표(또는 4분음표) 마무리 | 가사 음절이 촘촘한 구간 |
| 프레이즈 3번째 마디 | 8분음표 연속 4~5개 + 4분음표 1~2개, 일부 프레이즈는 마지막 2개 음에 **악센트(>)** | "울상을 짓다가", "퐄짝퐄짝콩콩콩" 구간처럼 후반부를 세게 강조 |
| 프레이즈 4번째 마디(마무리) | 점2분음표 + 4분쉼표 | 프레이즈 끝맺음, 여운 구간 |

- 코드 상수 예시(의사코드):
```
아기염소_패턴_4_4 = [
  // 1프레이즈
  ["E","E","E","E","Q","H"],       // 마디1
  ["E","E","E","E","E","E.","E"],  // 마디2: 촘촘한 8분음표 run
  ["E","E","E","E","Q"],           // 마디3
  ["DH","QR"],                      // 마디4: 점2분음표 + 4분쉼표
  // 2~6프레이즈도 동일 골격, 3번째 마디의 악센트(>) 위치만 다르게 부여
];
```
- **악센트 표시**: 스페이스바 입력으로는 세기(velocity)를 감지할 수 없으므로, 정확도 판정에는 반영하지 않되 화면에 `>` 기호와 강조 색을 표시하고 "이 박자는 특히 힘주어 쳐보세요" 안내 문구를 노출(시각적 학습 요소로만 사용)
- 이 프리셋은 **"악보 선택" 드롭다운에서 "아기 염소"를 선택했을 때 자동으로 4/4박자와 함께 로드**되며, 프리셋 이름은 "아기 염소 리듬"으로 표기

#### 3.1.2 "별 보며 달보며" 리듬 반영 (업로드 악보 분석, 6/8 전용 프리셋)
업로드된 악보(유성윤 작사·유병무 작곡, "별 보며 달보며")를 시각적으로 분석해 **음높이는 제외하고 리듬(음가)만** 추출했다. 6/8박자(한 마디 = 8분음표 6개 분량, 큰박 2개 느낌)이며, 총 4줄(각 줄 4마디, 총 16마디)로 구성되고 뒷부분에 점4분음표·붙임줄(타이)로 길게 끄는 음("—" 표기)이 자주 등장하는 것이 특징이다. *세부 음가는 육안 판독 기반 근사치이므로, 정밀한 채보가 필요하면 원곡과 재대조를 권장.*

| 마디 위치 | 리듬 패턴(좌→우) | 비고 |
|---|---|---|
| 1번째 마디(모든 줄 공통) | 4분음표, 8분음표×3(붙임beam), 8분음표 | "멀 리서반짝 이" 형태 (Q-E-E-E-E, 총 6개 8분 단위) |
| 2번째 마디 (1·2·4번째 줄) | 점4분음표, 4분음표, 8분쉼표 | "이는—" 형태로 한 음을 길게 끌고 8분쉼표로 숨 고르기 |
| 2번째 마디 (3번째 줄만 다름) | 4분음표+8분음표(붙임줄로 연결, 길게 끌기), 점4분음표 | "나 — 라" 형태, 특히 길게 끄는 구간 |
| 3번째 마디(모든 줄 공통) | 4분음표, 8분음표×2(붙임beam), 4분음표 | "별 님과 같" 형태 (Q-E-E-Q) |
| 4번째 마디(모든 줄 공통, 마무리) | 점4분음표+4분음표(붙임줄로 연결), 8분쉼표 | "이—" 형태로 마디를 넘어 길게 끌고 8분쉼표로 마무리 |

- 코드 상수 예시(의사코드):
```
별보며달보며_패턴_6_8 = [
  // 1번째 줄(1절: 멀리서 반짝이는 별님과 같이 —)
  ["Q","E","E","E","E"],        // 마디1: 멀-리서반짝-이
  ["DQ","Q","ER"],               // 마디2: 이는— (길게 끌고 숨고르기)
  ["Q","E","E","Q"],             // 마디3: 별-님과-같
  ["DQ~Q","ER"],                  // 마디4: 이— (붙임줄로 길게 끌며 마무리)
  // 2번째 줄: 1번째 줄과 리듬 동일(가사만 다름)
  // 3번째 줄(높푸른하늘나라 별님의나라): 마디2만 다름
  ["Q","E","E","E","E"],
  ["Q~E","DQ"],                   // 마디2: 나—라 (타이로 길게 끈 뒤 점4분음표)
  ["Q","E","E","Q"],
  ["DQ~Q","ER"],
  // 4번째 줄: 1번째 줄과 리듬 동일
];
```
- 이 프리셋은 **"악보 선택" 드롭다운에서 "별 보며 달보며"를 선택했을 때 자동으로 6/8박자와 함께 로드**되며, 프리셋 이름은 "별 보며 달보며 리듬"으로 표기
- 붙임줄(타이)로 연결된 두 음은 **하나의 긴 박자**로 취급하여, 그 구간 전체에서 스페이스바를 한 번만 누르고 길게 유지(또는 시작 시점 1회만 입력)하면 정타로 인정하도록 판정 로직에 예외 처리 필요(일반 음표처럼 각 음마다 따로 입력받지 않음)

#### 3.1.3 "손을 잡고" 리듬 반영 (업로드 악보 분석, 3/4 전용 프리셋)
업로드된 악보(권덕원 작사·미국 노래, "손을 잡고")를 시각적으로 분석했다. 3/4박자이며, 신체 활동(손잡고 돌기 등)을 위한 곡답게 "**4분음표-8분음표-8분음표-4분음표**"로 이어지는 셀이 반복적으로 등장하는 것이 특징이다.

| 리듬 셀 | 구성 | 등장 예 |
|---|---|---|
| 셀 A | Q(4분)-E(8분)-E(8분)-Q(4분) | "오[Q]른[E]손[E]과[Q]", "손[Q]을[E]잡[E]고[Q]" |
| 셀 B | Q-Q-E-E | "원[Q]손[Q]으[E]로[E]" |
| 셀 C(붙임) | Q~Q(타이)-E-E | "왼[Q]쪽[Q,타이로 길게]—으[E]로[E]" |
| 셀 D(마침) | Q-E-E-QR(4분쉼표) | "돌[Q]아[E]라[E]+쉼표" |
| 셀 E(종지) | H(2분)-QR | 각 프레이즈의 마지막 마디 |

- 코드 상수 예시(의사코드):
```
손을잡고_패턴_3_4 = [
  ["Q","E","E","Q"],   // 오른손과
  ["Q","Q","E","E"],   // 원손으로
  ["Q","E","E","Q"],   // 서로손잡고
  ["H","QR"],           // 고 (종지)
  // ...이하 같은 셀 구조 반복(가사만 다름), 마지막 줄은 "빙빙돌아라"로 "E-E-E-QR" 변형 포함
];
```
- *신체 활동 게임 특성상, 이 곡은 리듬 게임 화면에 "오른손/왼손/오른발/왼발" 등 동작 안내 텍스트를 함께 표시하면 학습 효과가 높음(선택적 기능으로 제안)*
- 이 프리셋은 **"손을 잡고"를 선택했을 때 자동으로 3/4박자와 함께 로드**

#### 3.1.4 "가을길" 리듬 반영 (업로드 악보 분석, 4/4 전용 프리셋)
업로드된 악보(김규환 작사·작곡, "가을길")를 시각적으로 분석했다. 4/4박자이며, 두 가지 대조적인 리듬 성격이 번갈아 나온다: ① 촘촘한 8분음표 run이 이어지는 "노랗게 노랗게 물들었네" 구간과, ② **완전히 균등한 4분음표만으로 이루어진** "가을길은 고운길" 후렴 구간.

| 구간 | 리듬 패턴 | 비고 |
|---|---|---|
| 도입부(1~2마디) | Q, E×4~5개 run, Q, QR | "노랗게노랗게 물들었네" |
| 후렴(3마디) | Q-Q-Q-Q | "가을길은" — 균등 4분음표만 사용 |
| 후렴(4마디) | Q-Q-Q-QR | "고운길" + 쉼표 |
| 의성어 구간("트랄랄랄라") | E-E-E-E 계열의 촘촘한 리듬 | 노래 부르기 전 추임새 구간, 선택적으로 반영 |

- 코드 상수 예시(의사코드):
```
가을길_패턴_4_4 = [
  ["Q","E","E","E","Q","QR"],   // 노랗게노랗게 물들었네(근사)
  ["Q","Q","Q","Q"],             // 가을길은  (균등 4분음표)
  ["Q","Q","Q","QR"],            // 고운길
  // ...
];
```
- 이 프리셋은 **"가을길"을 선택했을 때 자동으로 4/4박자와 함께 로드**되며, 프리셋 이름은 "가을길 리듬"으로 표기

#### 3.1.5 "악기놀이" 리듬 반영 (업로드 악보 분석, 12/8·자진모리장단 전용 프리셋)
업로드된 악보(권덕원 작사·작곡, "악기놀이")는 국악 장단인 **자진모리장단**을 익히는 곡으로, 12/8박자(한 마디 = 8분음표 12개, 큰박 4개로 느껴짐)이다. 원 악보에는 소고·북·꽹과리·장구 등 타악기를 위한 별도 리듬 줄(× 표시 음표)과 "동동동도동", "갠지개갱", "덩기덕쿵" 같은 **구음(전통 리듬 구술 음절)**이 함께 적혀 있다.

> **정확도 유의사항**: 이 곡은 국악 타악기 표기(×표 음표)와 구음이 포함되어 있어, 다른 곡들과 같은 방식(4분음표·8분음표 등 서양 음가 표기)으로 완전히 환원하기 어렵다. 아래 패턴은 자진모리장단의 전형적인 리듬감(큰박 4개, 각 큰박이 8분음표 3개로 나뉘는 구조)을 반영한 **근사치 표현**이며, 정확한 국악 장단을 가르치려면 국악 전문 자료로 별도 검증을 권장한다.

| 큰박 구조 | 리듬 패턴 | 비고 |
|---|---|---|
| 기본 셀 | E-E-E (8분음표 3개 = 점4분음표 1박) × 4묶음 | 자진모리 기본 골격 |
| 변형 셀 | Q-E (4분+8분, 3개 8분 분량을 2음으로 표현) | "친구들아" 등 가사가 성긴 구간 |
| 구음 표시 | 리듬은 동일하게 유지하되, 화면에 "동동동도동" 등 구음 텍스트를 함께 노출(정답 판정에는 미반영, 시각·청각 보조용) | 국악 리듬 교육 요소 |

- 코드 상수 예시(의사코드):
```
악기놀이_패턴_12_8 = [
  ["Q","E","E-E-E","E-E-E"],   // 친구들아 모여서 (근사)
  ["E-E-E","E-E-E","E-E-E","E-E-E"], // 악기놀이 해보자 (구음: 얼씨구절씨구)
  // ...
];
```
- 이 프리셋은 **"악기놀이"를 선택했을 때 자동으로 12/8박자와 함께 로드**되며, 프리셋 이름은 "악기놀이 리듬(자진모리)"으로 표기. 화면에 "자진모리장단"이라는 명칭도 함께 안내해 국악 장단 학습이라는 맥락을 알려줄 것

#### 3.1.6 "다섯 글자 예쁜말" 리듬 반영 (업로드 악보 분석, 4/4 전용 프리셋)
> **파일명 확인 요청**: 업로드하신 파일명은 "악보74_악기놀이.png"였으나, 실제 악보 이미지의 제목은 **"다섯 글자 예쁜말"**(정수은 작사·임수연 작곡)로 되어 있습니다. 파일명이 잘못 붙은 것으로 보여 아래에서는 악보에 표기된 실제 제목 기준으로 반영했습니다. 다른 곡을 의도하신 것이라면 알려주세요.

4/4박자이며, "있는—", "말—", "정—겨운" 처럼 **한 음을 길게 끄는 구간(붙임줄/2분음표)** 이 자주 나오는 서정적인 곡이다.

| 구간 | 리듬 패턴 | 비고 |
|---|---|---|
| 시작(쉼표+긴 음) | 4분쉼표, 2분음표 | "한 손"에서 "손"을 길게 끎 |
| 이어지는 구간 | Q-Q-Q-Q 계열 | "만으로도 세어" |
| 끝을 길게 끄는 구간 | Q~Q(타이) 또는 H, 이어서 QR | "볼수있는—" 종지 |

- 코드 상수 예시(의사코드):
```
다섯글자예쁜말_패턴_4_4 = [
  ["QR","H"],              // (쉼표) 한 손
  ["Q","Q","Q","Q"],       // 만으로 도세어
  ["Q","Q","H"],            // 볼수있는—
  ["DQ~Q","QR"],            // 종지
];
```
- 이 프리셋은 **"다섯 글자 예쁜말"을 선택했을 때 자동으로 4/4박자와 함께 로드**되며, 프리셋 이름은 "다섯 글자 예쁜말 리듬"으로 표기

### 3.2 메트로놈
- BPM 슬라이더: 범위 **60~160 BPM**, 기본값 90 BPM, 숫자 입력창 병행 제공
- ON/OFF 토글 버튼
- 메트로놈 ON 시: 매 박마다 짧은 클릭음(Web Audio API, 낮은 톤 "똑") + 상단 원형 인디케이터 깜빡임(4박 기준 1~4 숫자 순환 표시)
- *(3.7 참고) 메트로놈 클릭음은 "일정한 박"만 알려주는 용도이며, 실제 리듬(음표 길이)을 들려주는 소리는 아래 3.7에서 별도로 추가한다.*

### 3.3 리듬 악보 및 진행 표시
- 마디별 음표를 가로로 배치한 시각 악보 렌더링(SVG 또는 CSS로 음표 아이콘 표현)
- 재생 시작 시, 현재 박자 위치에 세로 바(플레이헤드) 또는 음표 배경 음영이 **BPM에 정확히 동기화되어 왼쪽→오른쪽으로 이동**
- 진행은 `setInterval`이 아닌 **오차 누적 방지를 위해 `performance.now()` 기반의 타이밍 계산** 사용 (코드 주석으로 설명 필수)

### 3.4 입력 및 판정
- 입력: **스페이스바(PC) + 화면 터치 버튼(모바일 대응)** 둘 다 지원
- **입력 시 즉시 사운드 재생**(3.7 참고): 판정 결과와 무관하게 스페이스바/터치를 누르는 즉시 3.7.1에서 선택한 드럼 소리가 짧게 재생되어 실제로 손뼉/타악기를 치는 듯한 느낌을 준다
- 판정 기준(예시, 코드에서 상수로 조정 가능하게 작성):
  - |오차| ≤ 50ms → "완벽!"
  - 50ms < |오차| ≤ 100ms → "굿"
  - 오차 > +100ms → "조금 느려요"
  - 오차 < -100ms → "조금 빨라요"
  - 해당 박자에 입력이 아예 없으면 "놓쳤어요"
- 쉼표 구간에서는 입력하지 않는 것이 정답 → 쉼표에서 스페이스바 입력 시 오답 처리

### 3.5 점수 및 통계
- 정확도(%) = (정타 수 / 전체 박자 수) × 100 (여기서 "정타"는 완벽+굿 판정 합산으로 정의, 코드 내 명시)
- 종료 후 통계 표시: 총 박자 수, 정확히 맞춘 횟수, 너무 빠름 횟수, 너무 느림 횟수, 놓친 횟수
- 등급 기준은 **2.4와 동일한 표 재사용**
- 등급별 시각 효과: A등급=색종이(confetti) CSS 애니메이션, B등급=별 반짝임, C/D등급=격려 문구+잔잔한 색상 전환(경쟁적 좌절감 방지를 위해 D등급도 긍정적 톤 유지)
- **게임 종료 시 3.6의 랭킹에 자동 기록**(닉네임 로그인 상태일 때)

### 3.6 닉네임 로그인 및 랭킹 (신규)

#### 3.6.1 닉네임 로그인
- 앱 최초 진입 시 또는 기능 B 탭에 처음 들어갈 때 **"닉네임을 입력해주세요" 모달**을 띄운다 (2~10자, 한글/영문/숫자만 허용하는 간단한 검증)
- 로그인한 닉네임은 화면 상단(탭 바 근처)에 항상 표시하고, 언제든 클릭해서 **"닉네임 변경"** 가능
- 이 로그인 역시 실제 계정 인증이 아니라 **랭킹에 표시할 이름을 정하는 용도**임을 안내 문구로 명시(비밀번호 없음, 같은 닉네임을 여러 명이 써도 막지 않음)
- 닉네임을 입력하지 않아도 기능 B를 플레이할 수는 있게 하되, 이 경우 "익명"으로 기록되거나 랭킹에는 기록되지 않도록 처리(둘 중 택1로 구현, "익명" 처리를 권장)

#### 3.6.2 랭킹 기록
- 기능 B의 한 라운드(문항 수 또는 마디 진행)가 끝나 점수가 나오면, 아래 정보를 랭킹에 자동 저장한다:
  - 닉네임, 점수(정확도%), 등급(A~D), 플레이한 콘텐츠(곡 선택 모드면 곡 제목, 기본 패턴 모드면 "기본 패턴(박자)"), 박자, 기록 시각
- 저장 방식은 **6장 교사용 콘텐츠와 동일하게 `window.storage`(shared:true)** 사용 — 반 전체 학생이 같은 랭킹을 봐야 하므로
  - 저장 키: `leaderboard:entries` (전체 기록을 배열 하나로 묶어 JSON 저장, 잦은 개별 호출 대신 한 번에 불러오고 한 번에 저장)
  - 새 기록 추가 시: 기존 배열을 불러와 새 항목을 추가 → 점수 내림차순 정렬 → **상위 100건까지만 남기고 저장**(무한정 누적 방지). 화면에 보여주는 개수(TOP 30, 3.6.3 참고)와는 별개로, 저장소에는 여유 있게 100건까지 보관해 필터별 통계 등에 활용 가능
  - 저장/불러오기는 try/catch로 감싸고 실패해도 게임 진행 자체는 막지 않음(랭킹 저장 실패 시 "랭킹 기록에 실패했어요" 정도의 조용한 안내만)

#### 3.6.3 랭킹 보드 UI
- 기능 B 화면에 "랭킹 보기" 버튼(또는 상시 노출되는 사이드 패널)을 두고, 클릭 시 랭킹 표를 보여준다
- 기본은 **전체 TOP 30**(점수 내림차순), 필터로 **"곡별 보기"**(드롭다운으로 특정 곡만 필터링)와 **"기본 패턴 모드만 보기"** 전환 가능
- 표시 항목: 순위, 닉네임, 점수, 등급, 플레이한 곡/패턴, 날짜
- 현재 로그인한 닉네임의 기록은 표에서 강조 색으로 하이라이트

#### 3.6.4 유의사항
- 진짜 로그인이 아니므로 실명이나 개인정보를 닉네임에 넣지 않도록 안내 문구를 로그인 모달에 표시
- (선택) 욕설·비속어 최소 필터링: 아주 단순한 금칙어 배열을 코드 상수로 두고 일치하면 "다른 닉네임을 입력해주세요"로 재입력 요청(완벽한 필터링은 아님을 명시)

### 3.7 사운드 구현 점검 및 보강 (신규)

> **점검 결과**: 기존 명세의 3.2(메트로놈)는 "일정한 박자 클릭음"만 정의되어 있었고, 실제 리듬 패턴(8분음표·점음표·쉼표 등 서로 다른 길이)을 소리로 들려주는 기능은 빠져 있었다. 아래를 추가로 명시한다.

1. **"리듬 미리듣기" 버튼(신규)**: 게임 시작 전이나 중간에 언제든 누르면, 현재 마디(또는 전체 진행 구간)의 리듬 패턴을 **음표 길이에 정확히 맞춰** 순서대로 사운드로 재생한다(아래 3.7.1의 선택된 드럼 사운드 사용). 쉼표 구간은 소리 없이 시간만 흐른다. BPM 설정과 정확히 동기화되어야 하며, 재생 중에는 화면의 진행 표시(3.3)도 함께 움직인다.
2. **사용자 입력 사운드(신규, 3.4에도 명시)**: 스페이스바/터치 입력 시 판정 결과가 나오기 전에 즉시 짧은 타격음(아래 3.7.1의 선택된 드럼 사운드)을 재생해, 실제로 리듬을 치는 듯한 청각 피드백을 준다.
3. **음색 구분**: 메트로놈 클릭(낮은 톤 "똑", 고정)과 리듬 미리듣기·사용자 입력 사운드(3.7.1에서 고른 드럼 소리, 서로 동일한 음색 사용)가 헷갈리지 않도록 서로 다른 주파수 대역으로 구현한다.

#### 3.7.1 드럼 소리 선택 (신규)
기능 B 화면에 **"드럼 소리 선택"** 버튼 그룹(또는 드롭다운)을 두어, 아래 3종 중 하나를 골라 리듬 미리듣기·사용자 입력 사운드에 공통 적용한다. 실제 드럼 음원 파일은 쓰지 않고, Web Audio API만으로 합성한다.

| 옵션 | 이름 | 합성 방식(의사코드 수준) | 느낌 |
|---|---|---|---|
| 1 | **우드블록** | 사각파(square) 오실레이터, 800~1000Hz, 아주 짧은 감쇠(50ms 이내 지수적 볼륨 감쇠) | "딱" 하는 짧고 단단한 소리 |
| 2 | **스네어** | `AudioBufferSourceNode`로 화이트 노이즈를 생성한 뒤 밴드패스 필터(1000~3000Hz)를 걸고 100ms 이내로 빠르게 감쇠 | "치직/착" 하는 스네어 느낌 |
| 3 | **탐탐(북)** | 사인파 오실레이터, 시작 주파수 150Hz에서 80Hz로 빠르게 하강(pitch envelope) + 150ms 정도의 감쇠 | "퉁/둥" 하는 낮은 북 소리 |

- 선택한 드럼 소리는 세션 동안 유지되며(새로고침하면 기본값인 "우드블록"으로 복귀), 화면에 현재 선택된 소리가 강조 표시되게 한다
- 각 옵션 옆에 작은 "미리듣기" 버튼을 두어, 게임을 시작하지 않고도 소리만 먼저 들어볼 수 있게 한다
- (선택) 6장 교사용 페이지에서 반 전체의 기본 드럼 소리를 지정하고 싶다면, `teacher_content:settings`에 `defaultDrumSound` 필드를 추가해 저장하는 방식으로 확장 가능

---

## 4. 기능 C: 악보 이미지 업로드 → 리듬 악보 자동 생성 및 다운로드 (신규, 새 탭)

### 4.1 목적
사용자가 오선보 악보 이미지를 업로드하면, **음높이는 제외하고 리듬(음가)만 추출**하여 기능 B에서 쓰는 것과 같은 형식의 "리듬 전용 악보"로 재구성하고, 이를 이미지 파일로 다운로드할 수 있게 한다.

### 4.2 업로드 및 인식
- 파일 업로드 입력(input type=file, accept="image/*")으로 악보 이미지(PNG/JPG) 1장 업로드
- **인식 방식**: 브라우저 자바스크립트만으로는 악보 이미지에서 리듬을 자동으로 읽어내는 것(광학 악보 인식, OMR)이 사실상 불가능하므로, **Anthropic API(멀티모달, 이미지 입력 지원)를 호출**해 이미지를 분석하고 마디별 음표/쉼표 리듬을 JSON으로 반환받는 방식을 사용한다.
  - 프롬프트 설계 예: "이 악보 이미지에서 음높이는 무시하고 마디별 리듬(음가)만 4분음표/8분음표/점4분음표/2분음표/쉼표/타이 기호로 분류해 JSON 배열로만 응답하라"
  - 응답은 반드시 JSON만 받도록 시스템 프롬프트에 명시(코드 내 파싱 시 마크다운 코드펜스 제거 로직 포함)
  - 이 앱이 Claude 아티팩트 환경에서 실행되는 경우 `window.fetch("https://api.anthropic.com/v1/messages", ...)` 로 별도 API 키 없이 호출 가능(이미지는 base64로 인코딩해 `image` 콘텐츠 블록으로 전달); 그 외 독립 배포 환경이라면 사용자가 직접 API 키를 입력하거나 백엔드 프록시를 구성해야 함을 안내 문구로 표시
- **인식 정확도 한계 안내**: 화면에 "자동 인식 결과는 참고용입니다. 실제 악보와 다를 수 있으니 아래에서 확인 후 필요한 부분을 수정해주세요"라는 문구를 항상 노출

### 4.3 인식 결과 검토 및 수정
- 인식된 리듬을 기능 B와 동일한 표기(음표/쉼표 아이콘)로 화면에 마디별로 렌더링
- 각 음표를 클릭하면 다른 음가(4분음표/8분음표/점4분음표/2분음표/점2분음표/4분쉼표/8분쉼표)로 바꿀 수 있는 간단한 팝업 선택 UI 제공(오인식 교정용)
- 마디 추가/삭제 버튼 제공

### 4.3-1 소리로 확인하기 (신규)
> **점검 결과**: 기존 명세에는 인식된 리듬을 소리로 들어볼 방법이 없어, 사용자가 인식 결과가 실제 악보와 맞는지 청각적으로 확인할 수 없었다. 아래를 추가한다.
- "재생" / "정지" 버튼을 추가해, 검토·수정이 끝난 리듬을 처음부터 끝까지 **3.7의 "리듬 미리듣기"와 같은 사운드 엔진**으로 재생
- 재생 중에는 현재 재생 중인 음표에 하이라이트 표시(기능 B의 진행 표시 로직 재사용)
- 사용자가 눈(악보 모양)과 귀(소리) 두 가지로 인식 결과를 검증한 뒤 다운로드하도록 유도

### 4.4 다운로드
- "리듬 악보 다운로드" 버튼: 현재 화면에 렌더링된 리듬 악보를 **PNG 이미지**로 캡처해 다운로드 (Canvas API의 `toDataURL` 또는 SVG를 Canvas에 그린 뒤 변환하는 방식 사용, 외부 라이브러리 없이 구현)
- 파일명 규칙: `리듬악보_YYYYMMDD_HHmm.png` 형태로 자동 지정
- (선택 기능) "이 리듬을 리듬 치기 게임에 저장" 버튼을 추가해, 인식·수정된 리듬을 기능 B의 "곡 선택 모드" 목록에 임시 프리셋으로 즉시 추가할 수 있게 하면 두 기능이 자연스럽게 연동됨

---

## 5. 기능 D: 오선보 작곡기 — 클릭으로 악보 구성 + 재생/정지 (신규, 새 탭)

### 5.1 목적
학생이 높은음자리표 오선보 위에 음표·쉼표 버튼을 클릭해 직접 짧은 악보를 만들어보고, 만든 악보를 즉시 재생해서 들어볼 수 있게 한다.

### 5.2 화면 구성
- 상단: 높은음자리표가 그려진 오선보 영역(마디선으로 구분, 기본 4마디 표시, "마디 추가"/"마디 삭제" 버튼으로 조절 가능)
- 하단: 입력 팔레트(버튼 그룹)
  - 음가 선택: 온음표, 점2분음표, 2분음표, 점4분음표, 4분음표, 8분음표, 16분음표, 4분쉼표, 8분쉼표 (총 9종)
  - 지우개(선택한 음표 삭제) / 되돌리기(Undo) 버튼

### 5.3 입력 방식
1. 팔레트에서 원하는 음가를 먼저 선택(선택된 음가 버튼은 강조 표시)
2. 오선보의 원하는 줄/칸 위치를 클릭하면, 그 위치의 음높이(도~높은 도 범위, 필요 시 옥타브 위아래로 낮은음자리표 없이 덧줄 몇 개까지만 확장) + 선택된 음가로 음표가 삽입됨
3. 마디의 박자 수를 초과해서 삽입하려 하면 경고 메시지("이 마디에 더 이상 넣을 수 없어요")를 띄우고 삽입을 막음(4/4박자 기준으로 마디당 4박 초과 방지, 박자 계산 로직 재사용)
4. 이미 음표가 있는 위치를 다시 클릭하면 삭제(토글) 처리

### 5.4 재생/정지
> **점검 결과**: 이 기능은 처음부터 재생/정지가 명세에 포함되어 있었으므로 별도 보강이 필요 없다. 아래 내용 그대로 유지.

- **재생 버튼**: 클릭 시 구성된 악보를 첫 음부터 순서대로 Web Audio API(OscillatorNode)로 실제 음높이(도~시)와 음가에 맞춰 재생. 재생 중인 음표 위에 하이라이트(기능 B의 진행 표시 로직 재사용) 표시
- **정지 버튼**: 재생 중 언제든 클릭하면 즉시 소리와 하이라이트 진행을 멈춤(정지 시점의 오실레이터를 확실히 `stop()` 처리해 소리가 끊기지 않고 남지 않도록 주의)
- 템포(BPM) 슬라이더 제공(기본 100 BPM, 범위 60~160), 재생 중에는 비활성화하고 정지 후 다시 조절 가능
- 재생이 끝까지 진행되면 자동으로 재생 버튼 상태로 복귀

### 5.5 저장/내보내기(선택 기능)
- 작곡한 악보를 기능 C와 같은 방식으로 이미지 다운로드할 수 있는 버튼을 추가하면 두 기능 간 일관성이 생김(선택 사항으로 제안)

---

## 6. 교사용 페이지 (비밀번호 로그인 후 콘텐츠 관리, 신규)

### 6.1 목적 및 성격
교사가 학생들에게 보여주는 4개 탭(A/B/C/D)의 콘텐츠(리듬 게임 곡 목록, 기본 패턴, 문항 수·BPM·타이밍 허용오차 등 설정값)를 코드를 직접 건드리지 않고 화면에서 추가·수정·삭제할 수 있게 하는 관리자 페이지다.

> **보안 성격에 대한 중요한 안내**: 이 로그인은 학생들이 실수로 설정을 건드리지 않도록 막는 **간단한 화면 잠금 장치**이며, 실제 계정 인증이나 서버 보안이 아니다(비밀번호가 클라이언트 코드 안에 존재하므로 마음만 먹으면 우회 가능). 민감한 개인정보나 진짜 보안이 필요한 용도로는 사용하지 말 것을 프롬프트에도 명시해야 한다.

### 6.2 접근 방법
- 화면 우측 상단의 **자물쇠 아이콘("교사용")** 클릭 → 비밀번호 입력 모달 표시
- 기본 비밀번호는 코드 상수(`const TEACHER_PASSWORD = "0000"` 형태)로 지정하고, 이 상수 하나만 바꾸면 비밀번호를 변경할 수 있도록 구현(교사가 코드에서 쉽게 찾아 바꿀 수 있게 주석으로 위치 안내)
- 비밀번호가 맞으면 "교사용" 탭이 활성화되어 열림. 새로고침하면 다시 잠김(세션 내에서만 잠금 해제 유지)
- 5회 연속 오답 시 "잠시 후 다시 시도해주세요"와 함께 30초간 재입력 버튼 비활성화(무차별 대입 완화용 최소한의 장치, 진짜 보안은 아님을 재차 인지)

### 6.3 교사용 페이지 구성 및 편집 가능 항목
1. **곡 관리(기능 B "곡 선택 모드" 대상)**
   - 목록 표(현재 등록된 곡: 아기 염소, 별 보며 달보며, 손을 잡고, 가을길, 악기놀이, 다섯 글자 예쁜말 + 교사가 추가한 곡)에서 각 곡을 수정/삭제
   - "새 곡 추가" 폼: 곡 제목 입력, 박자 선택(2/4·3/4·4/4·6/8·12/8 드롭다운), 마디별 리듬을 쉼표/파이프로 구분한 텍스트로 입력(예: `Q,E,E,Q|H,QR` = 1마디에 4분·8분·8분·4분, 2마디에 2분·4분쉼표), 입력값을 실시간으로 미리보기 렌더링해 오타를 바로 확인 가능하게 함
2. **기본 패턴 관리(기능 B "기본 패턴 모드" 대상)**: 박자별 기본 패턴 배열에 새 패턴 추가/삭제
3. **설정값 조정**: 아래 항목을 슬라이더 또는 숫자 입력으로 조정
   - 기능 A 문항 수 옵션(현재 5/10/15), 등급 기준 점수 구간(현재 90/70/50)
   - 기능 B·D 공통 BPM 범위(현재 60~160), 타이밍 허용 오차(현재 ±50ms/±100ms)
4. **초기화 버튼**: "기본값으로 되돌리기"를 누르면 교사가 추가/수정한 내용을 모두 지우고 문서에 정의된 기본 콘텐츠(6곡 + 기본 패턴)로 복원

### 6.4 데이터 저장 방식 (영구 저장)
- 이 앱이 **Claude 아티팩트 환경에서 실행되는 경우**, `window.storage`의 키-값 저장 API를 사용해 교사가 등록한 콘텐츠를 저장한다. 학생과 교사가 같은 콘텐츠를 봐야 하므로 **`shared: true`**로 저장해 모든 사용자에게 공유되게 한다.
  - 저장 키 설계(한 번에 묶어서 저장해 호출 횟수 최소화):
    - `teacher_content:songs` → 교사가 추가/수정한 곡 배열 전체를 JSON 문자열로 저장
    - `teacher_content:base_patterns` → 박자별 기본 패턴 배열 전체
    - `teacher_content:settings` → 문항 수, BPM 범위, 타이밍 허용 오차, 등급 기준 점수 등 설정값 객체
  - 저장/불러오기 예시(의사코드):
    ```javascript
    // 저장 (교사가 "저장" 버튼 클릭 시)
    try {
      await window.storage.set('teacher_content:songs', JSON.stringify(songsArray), true);
    } catch (e) { /* 저장 실패 안내 문구 표시 */ }

    // 앱 시작 시 모든 사용자가 불러오기
    try {
      const result = await window.storage.get('teacher_content:songs', true);
      const savedSongs = result ? JSON.parse(result.value) : [];
      // 문서에 정의된 기본 6곡 + savedSongs 를 합쳐 드롭다운 구성
    } catch (e) {
      // 저장된 값이 없으면(최초 실행) 기본 6곡만 사용
    }
    ```
  - 모든 storage 호출은 try/catch로 감싸고, 불러오는 동안 "설정을 불러오는 중..." 같은 로딩 표시를 잠깐 보여줄 것
- 이 앱을 **Claude 아티팩트 환경 밖(자체 서버 배포 등)에 올리는 경우**에는 `window.storage`를 쓸 수 없으므로, 대안으로 `localStorage`를 사용하되 **그 기기·브라우저에만 저장되고 다른 학생 기기와 공유되지 않는다는 한계**를 코드 주석과 UI 안내 문구로 명시할 것

---

## 7. 기술적 요구사항

- **구현 방식: 순수 HTML + CSS + Vanilla JavaScript (단일 파일, 외부 프레임워크/라이브러리 없이 동작)**
  - 이유: 별도 빌드 과정 없이 바로 열어서 테스트 가능해야 함
  - 예외: **기능 C(악보 인식)** 는 `fetch`로 Anthropic API를 호출하는 부분만 네트워크 통신이 필요하며, 그 외 모든 로직은 순수 클라이언트 사이드로 유지
- 오디오: Web Audio API의 `OscillatorNode`로 음 생성 (외부 음원 파일 사용 금지)
- 이미지 생성/다운로드(기능 C, D): Canvas API의 `toDataURL('image/png')`와 `<a download>` 링크 조합으로 구현, 외부 라이브러리 없이 처리
- **영구 저장(교사용 페이지 + 랭킹)**: `window.storage`의 `get`/`set`/`delete`/`list` API 사용, 교사 콘텐츠와 랭킹 모두 `shared: true`로 저장(반 전체가 공유). 데이터가 함께 갱신되는 항목(예: 곡 배열 전체, 랭킹 배열 전체)은 여러 번 나눠 호출하지 않고 하나의 키에 묶어 저장. 모든 호출은 try/catch로 감싸고 실패 시 사용자에게 안내(게임 진행 자체는 막지 않음)
- **사운드 3종 구분**(3.7): 메트로놈 클릭 / 리듬 미리듣기·사용자 입력용 드럼 소리(3.7.1, 우드블록·스네어·탐탐 3종 중 선택)를 서로 다른 방식으로 합성. 스네어는 `AudioBufferSourceNode` + 화이트 노이즈 + `BiquadFilterNode`, 나머지는 `OscillatorNode` 기반
- 레이아웃: CSS Grid/Flexbox 반응형, 모바일에서는 세로 스택 / 데스크탑에서는 여유 있는 2단 배치
- 타이밍 정확도: `requestAnimationFrame` + `performance.now()` 조합으로 애니메이션과 판정 로직 분리
- 코드 내 주석 필수 항목:
  - 오선보 좌표 매핑 테이블
  - 메트로놈 타이머 로직
  - 스페이스바/터치 입력 이벤트 핸들러
  - 타이밍 오차 계산 함수
  - 점수·등급 계산 함수
  - **Anthropic API 호출 및 JSON 파싱 로직(기능 C)**
  - **오선보 클릭 좌표 → 음높이 변환 로직, 재생 시퀀서 로직(기능 D)**
  - **교사용 비밀번호 상수 위치, `window.storage` 저장/불러오기 로직(6장)**
  - **닉네임 로그인 검증 로직, 랭킹 정렬·상위 100건 유지 로직(3.6)**
- 코드는 기능별로 섹션 주석(`// ===== 기능 A: 오선보 읽기 =====` 등)으로 명확히 구분(A/B/C/D/교사용 5개 섹션)

---

## 8. 출력 형식 (요청 시 지켜야 할 답변 구조)

1. **구조 개요**: 페이지 섹션 구성과 네 기능(A/B/C/D) + 교사용 페이지의 상태(state) 설계를 간단한 다이어그램 또는 목록으로 먼저 설명
2. **전체 코드**: 하나의 실행 가능한 HTML 파일(내부에 `<style>`, `<script>` 포함)로 전체 제공 (복사 후 바로 브라우저 실행 가능해야 함)
3. **코드 이후 설명**:
   - 리듬 패턴을 추가/변경하는 방법 (어느 배열/객체를 수정하면 되는지 구체적 변수명 명시)
   - **새로운 곡을 "악보 선택" 드롭다운에 추가하는 방법** (곡 제목, 박자, 마디별 패턴 배열을 어디에 등록하면 되는지 구체적 변수명·구조 명시)
   - 템포 범위(BPM min/max)와 타이밍 허용 오차(ms)를 조정하는 방법 (해당 상수 위치 명시)
   - 음역(옥타브) 확장이나 낮은음자리표 추가 시 수정이 필요한 부분 간단 안내
   - **기능 C를 Claude 아티팩트 환경 밖(자체 서버 배포 등)에서 쓸 때 API 키를 어떻게 연결해야 하는지** 간단 안내
   - **교사용 비밀번호를 바꾸는 방법**, 그리고 **Claude 아티팩트 환경 밖에 배포할 때 `window.storage` 대신 `localStorage`로 대체하는 방법**
   - **랭킹 보관 개수(상위 100건)를 조정하는 방법**, 랭킹을 초기화하고 싶을 때 어떻게 하면 되는지

---

## 개선 시 반영한 주요 변경 사항 (참고용)
- 원문의 "높은음자리표(또는 낮은음자리표)" 같은 **양자택일/모호한 표현을 하나로 확정**하고, 확장 여지는 별도로 명시
- 키보드 입력 매핑(도→C, 레→D…)을 **표로 명확히 고정**하여 구현 시 해석 차이 방지
- "일정 문제 수(예: 10문제)"처럼 예시로만 제시된 값을 **기본값+선택 옵션**으로 구체화
- 리듬 패턴이 "무엇을 기준으로 얼마나 다양하게" 나오는지 불명확했던 부분을 **사전 정의된 패턴 배열 + 무작위 추출** 방식으로 구체화
- 기능 A와 B에 각각 따로 있던 등급 기준을 **하나의 공통 표로 통일**해 일관성 확보
- 프레임워크가 미정이었던 부분을 **순수 HTML/CSS/JS 단일 파일**로 확정(즉시 실행 가능성 우선)
- 타이밍 정확도 이슈(`setInterval` 누적 오차)를 방지하기 위한 구체적 구현 방식(`performance.now()` 기반)을 기술 요구사항에 명시
- 모바일 사용자를 위해 스페이스바 외 **터치 입력 대안**을 추가
- 등급이 낮아도 학습 동기를 꺾지 않도록 **D등급 메시지·연출의 긍정적 톤**을 강조

## 개정 이력
- **v2**: 리듬 게임의 박자를 4/4 고정에서 **2/4·3/4·4/4·6/8 선택형**으로 확장. 사용자가 업로드한 "아기 염소" 악보를 시각 분석해 실제 리듬(8분음표 run, 4분음표, 점2분음표+쉼표 마무리, 후렴부 악센트 등)을 4/4 전용 프리셋으로 반영. 이에 맞춰 사용 음표/쉼표 종류를 3종에서 7종(점4분음표, 점2분음표, 2분음표, 악센트 포함)으로 확장
- **v3**: **"악보 선택" 드롭다운**을 신설해 곡 제목(현재 "아기 염소", "별 보며 달보며")을 고르면 해당 곡의 박자·리듬이 자동 로드되도록 "곡 선택 모드"를 "기본 패턴 모드"와 분리 추가. 사용자가 업로드한 "별 보며 달보며" 악보(6/8박자)를 시각 분석해 리듬을 프리셋으로 반영, 붙임줄(타이)로 길게 끄는 음가가 새로 등장함에 따라 음표/쉼표 종류를 7종에서 **8종(붙임줄 추가)**으로 확장하고, 타이 구간의 입력 판정 예외 규칙을 명시
- **v4**: "손을 잡고"(3/4), "가을길"(4/4), "악기놀이"(12/8·자진모리장단), "다섯 글자 예쁜말"(4/4, 업로드 파일명은 "악기놀이"였으나 실제 악보 제목 기준으로 등록) 4곡을 추가 분석해 "악보 선택" 드롭다운을 총 6곡으로 확장. 선택 가능 박자에 **12/8**을 추가(총 5종: 2/4·3/4·4/4·6/8·12/8)하고, 8분쉼표를 음표/쉼표 목록에 추가(총 9종). "악기놀이"는 국악 자진모리장단·타악기 구음이 포함된 곡이라 서양 음가로 완전히 환원하기 어려움을 명시하고 근사 패턴으로 반영
- **v5**: 신규 기능 2종을 추가 탭으로 확장(총 4탭 구성: A/B/C/D). **기능 C**: 악보 이미지를 업로드하면 Anthropic API(멀티모달)로 리듬만 추출해 리듬 전용 악보로 재구성하고 PNG로 다운로드, 오인식 교정 UI 포함. **기능 D**: 높은음자리표 오선보에 음표·쉼표 팔레트를 클릭해 직접 악보를 구성하고, 재생/정지 버튼으로 Web Audio API 재생이 가능한 오선보 작곡기. 이에 맞춰 기술적 요구사항에 Anthropic API 호출·이미지 다운로드(Canvas) 구현 방식을 추가
- **v6**: **비밀번호 로그인 기반 "교사용" 관리 페이지**를 신설(6장). 교사가 곡 목록·기본 패턴·BPM/타이밍/문항수 등 설정값을 화면에서 추가·수정·삭제할 수 있게 하고, 이 로그인이 진짜 보안이 아닌 화면 잠금 수준임을 명시. 교사가 등록한 콘텐츠는 세션이 끝나도 유지되어야 하므로 `window.storage`(공유 저장, `shared:true`) 기반 영구 저장 방식을 도입하고, 아티팩트 환경 밖 배포 시 `localStorage` 대체 방안도 함께 안내. 이에 따라 공통 요구사항의 "상태 관리" 항목과 기술적 요구사항·출력 형식을 업데이트
- **v7**: **닉네임 로그인 + 랭킹 기능**을 신설(3.6). 학생이 닉네임을 입력해 로그인하면 기능 B(리듬 치기 게임) 점수가 `window.storage`(공유)에 자동 기록되고, TOP 10/곡별 필터가 있는 랭킹 보드에서 확인 가능. **기존 명세의 사운드 구현 여부를 점검한 결과, 기능 B는 "일정한 메트로놈 클릭음"만 있고 실제 리듬(음표 길이)을 들려주는 소리가 빠져 있어 3.7을 신설해 "리듬 미리듣기" 버튼과 입력 시 사운드 피드백을 추가**하고, 서로 다른 음색으로 메트로놈/미리듣기/입력음을 구분하도록 명시. **기능 C(리듬 악보 생성)도 인식 결과를 소리로 들을 방법이 없었기에 4.3-1을 신설해 재생/정지 버튼을 추가**. 기능 D는 처음부터 재생/정지가 명세되어 있었음을 재확인(변경 없음)
- **v8**: 랭킹 보드에 표시되는 개수를 **TOP 10 → TOP 30**으로 확대(3.6.3, 저장소에는 여유 있게 상위 100건 계속 보관). **리듬 치기 게임(및 리듬 미리듣기)에서 쓰는 타격음을 우드블록/스네어/탐탐 3종 드럼 소리 중 선택할 수 있는 "드럼 소리 선택" 기능을 신설**(3.7.1)하고, 각 소리의 Web Audio API 합성 방식(오실레이터 vs 화이트노이즈+필터)을 구체적으로 명시
